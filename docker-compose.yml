services:
  webui:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.py:/app/config.py
    command: >
      sh -c "
      cd /app && git pull origin prevent-daytrading && python /app/webui.py"

  ranking:
    build:
      context: .
    volumes:
      - ./config.py:/app/config.py
    command: >
      sh -c "
      cd /app && git pull origin prevent-daytrading && python /app/ranking_client.py"

  trading:
    build:
      context: .
    volumes:
      - ./config.py:/app/config.py
    command: >
      sh -c "
      cd /app && git pull origin prevent-daytrading && python /app/trading_client.py"
